version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app/backend
      - ./data:/app/data
      # 모델은 로컬에서만 (너무 커서)
      - ./backend/models:/app/backend/models
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
